# KPP/bin

This folder is where the KPP executable file will be placed.

You will need to add the following lines to your .bash_aliases file:

    export KPP_HOME=/path/to/KPP
    export KPP_BIN=${KPP_HOME}/bin
    export PATH="${PATH}:${KPP_BIN}


